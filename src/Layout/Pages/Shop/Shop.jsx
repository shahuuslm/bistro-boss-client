import { Helmet } from "react-helmet";

const Shop = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro Boss || Shop</title>
            </Helmet>
            
        </div>
    );
};

export default Shop;